// possibly the component to display the grocery lists?

import React from 'react';
import './style.css';


const GrocList = () => {
  return (
    <div class="groceryList">
      <h1>Grocery List</h1>

      {/* maybe we can use a react table library.
      was looking into "material-table"
      https://github.com/mbrn/material-table
      with this code:
      npm install material-table @material-ui/core --save
       */}

       {/* we could also just start with a simple
       table with some css? */}
    </div>
  )
}

export default GrocList;