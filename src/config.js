// Global variables to keep track of user signin
// Boolean for status and Username String for endpoint connection
const AppContext = {
    isAuthenticated: false,
    username: ''
};

export default AppContext;